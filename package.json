{
  "name": "iphone-fleet-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "corepack pnpm -r --if-present build",
    "typecheck": "corepack pnpm -r --if-present typecheck",
    "lint": "biome check .",
    "unit": "vitest run --config vitest.unit.config.ts",
    "contract": "vitest run --config vitest.contract.config.ts",
    "mcp:black-box": "corepack pnpm --filter @iphone-fleet/fleet-mcp build && vitest run --config vitest.mcp-black-box.config.ts",
    "integration:local": "vitest run --config vitest.integration.config.ts",
    "integration": "corepack pnpm integration:local && corepack pnpm integration:postgres && corepack pnpm integration:hatchet && corepack pnpm integration:openadapt",
    "integration:postgres": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/windows/test-postgres.ps1",
    "integration:hatchet": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/windows/test-hatchet.ps1",
    "integration:openadapt": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/windows/test-openadapt.ps1",
    "e2e-mock": "vitest run --config vitest.e2e-mock.config.ts",
    "verify:windows": "corepack pnpm build && corepack pnpm typecheck && corepack pnpm lint && corepack pnpm unit && corepack pnpm contract && corepack pnpm mcp:black-box && corepack pnpm integration:local && corepack pnpm e2e-mock",
    "verify:wsl": "corepack pnpm integration:postgres && corepack pnpm integration:hatchet && corepack pnpm integration:openadapt",
    "verify": "corepack pnpm verify:windows && corepack pnpm verify:wsl",
    "real-ios:e2e": "node -e \"console.error('BLOCKED: macOS Device Lab and real iPhone are required (M0+).'); process.exit(2)\""
  },
  "engines": {
    "node": "24.14.1",
    "pnpm": "11.19.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@types/node": "26.4.1",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
