# Architecture Decision Records

ADR 用于记录影响架构的持久性决定。文件名格式为 `NNNN-kebab-case-title.md`。

状态使用 `Proposed`、`Accepted`、`Rejected`、`Superseded`。涉及新复杂通用组件或自研框架的 ADR，必须关联 OSS Evaluation；需要人工批准的 ADR 在批准前保持 `Proposed`，不得据此实施。

本目录的决定以 Architecture Baseline 为准；若发生冲突，必须先修订 Baseline 或通过 ADR 明确替代关系。
