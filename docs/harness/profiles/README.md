# Harness profile examples

These files are documentation examples, not auto-loaded project configuration. Replace absolute paths only after a Harness installation is explicitly approved; build Fleet MCP first. Do not add credentials to this repository.

Both profiles start the same `apps/fleet-mcp/dist/main.js` stdio server. They do not contain Harness-specific Fleet logic.
